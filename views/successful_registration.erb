<!DOCTYPE html>
<html>
<head>
  <title>Registration Success</title>
  <style>
    body {
      background-image: url('https://cdn.pixabay.com/photo/2018/08/14/13/23/ocean-3605547_1280.jpg');
      background-repeat: no-repeat;
      background-size: cover;
      background-position: center;
      font-family: Arial, sans-serif;
    }
    .container {
      max-width: 600px;
      margin: 0 auto;
      padding: 40px;
      background-color: transparent;
      border-radius: 10px;
      text-align: center;
      color: #fff;
    }
    h1 {
      color: #fff;
      font-size: 28px;
    }
    p {
      color: #fff;
      font-size: 18px;
      margin-bottom: 20px;
    }
    button {
      background-color: #4CAF50;
      color: #fff;
      padding: 12px 24px;
      border: none;
      border-radius: 4px;
      font-size: 16px;
      cursor: pointer;
    }
    button:hover {
      background-color: #45A049;
    }
  </style>
</head>
<body>
  <div class="container">
    <h1>Successfully Registered!</h1>
    <p>Congratulations, you have successfully registered.</p>
    <p>Current weather for London:</p>
    <% require 'net/http' %>
    <% require 'json' %>
    <% uri = URI('https://api.openweathermap.org/data/2.5/weather?q=London&appid=96f90464486a2b0a8de3da89b57b8df4') %>
    <% response = Net::HTTP.get(uri) %>
    <% weather_data = JSON.parse(response) %>
    <% temperature = weather_data['main']['temp'] - 273.15 %>
    <% weather_description = weather_data['weather'][0]['description'] %>
    <p>Temperature: <%= temperature.round(2) %>°C</p>
    <p>Weather Description: <%= weather_description.capitalize %></p>
    <p>Click the button below to view properties.</p>
    <form action="/properties" method="GET">
      <button type="submit">View Properties</button>
    </form>
  </div>
</body>
</html>

